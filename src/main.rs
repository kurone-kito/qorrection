//! qorrection — PTY wrapper that intercepts Vim-style quit
//! commands and responds with playful animations.
//!
//! This is a placeholder entry point. The PTY wrapper, trigger
//! detection, and animation renderer will be added in subsequent
//! changes per the project plan.

fn main() {
    eprintln!("qorrection: implementation pending");
    std::process::exit(2);
}
